(ns layouts
  (:require dommy :as dom))

; select, replace!, getById, getByClass

(defn chapter [content]
 you-are-here (:title content)
 preface's style
 section's style
 next-section link
  )

(defn section [content]
 (comment
    might have,
    aside
    datagram
    exercise
    ass
    footnote))

; viz, art, data, enchiladas
; (defmacro datagram [name & body])

(comment
  (defn art [search-term]
    image tag
    call to data
    which should cache
    and be nice
    and async
    ))
